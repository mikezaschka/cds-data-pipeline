// This example has no local consumer service — both source and target
// are remote OData. The only local "service" is the Pipeline Monitor
// annotations on the plugin's management service, imported below so the
// FE app renders correctly.
using from 'cds-data-pipeline/srv/monitor-annotations';
